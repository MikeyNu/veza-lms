export default {
  version: 1,
  status: "authoritative",
  workflows: [
    {
      id: "identity.invitation-to-workspace",
      name: "Invitation to active workspace",
      actors: ["invited user", "institution administrator"],
      entry: "A one-time membership invitation is delivered to a verified email address.",
      exit: "The accepted membership is reverified and installed as the active workspace selector.",
      browserRoute: "apps/web/app/invitation",
      failureStates: ["invalid link", "expired token", "revoked token", "wrong identity", "expired session", "service unavailable"],
      steps: [
        { id: "invitation-render", action: "Render the invitation context without exposing the token to analytics or referrers.", paths: ["apps/web/app/invitation/page.tsx"] },
        { id: "identity-sign-in", action: "Authenticate through the institution identity provider and return through a bounded safe path.", paths: ["apps/web/app/sign-in/page.tsx", "apps/web/app/api/auth/sign-in/route.ts"] },
        { id: "invitation-accept", action: "Verify email, token digest, expiry, status and delegation before atomically activating membership and role evidence.", paths: ["apps/web/app/api/invitations/accept/route.ts", "apps/api/src/modules/identity-access"] },
        { id: "workspace-install", action: "Install only the accepted membership selector and reverify it through the workspace session boundary.", paths: ["apps/web/src/server/require-workspace-session.ts", "apps/api/src/modules/identity-access"] },
      ],
      tests: ["apps/web/tests/identity-journeys.test.mjs", "apps/api/tests"],
    },
    {
      id: "identity.account-recovery",
      name: "Account recovery and retry",
      actors: ["institution user", "identity provider"],
      entry: "A user cannot complete institution sign-in or MFA.",
      exit: "The user completes identity-provider recovery and starts a fresh Veza sign-in.",
      browserRoute: "apps/web/app/account-help",
      failureStates: ["recovery URL not configured", "identity provider unavailable", "membership still unavailable"],
      steps: [
        { id: "classify-access-problem", action: "Separate credential recovery from institutional membership administration.", paths: ["apps/web/app/account-help/page.tsx"] },
        { id: "trusted-recovery-handoff", action: "Open only the configured HTTPS identity-provider recovery address.", paths: ["apps/web/src/server/identity-provider-links.ts", "apps/web/app/reset-password/page.tsx"] },
        { id: "fresh-sign-in", action: "Discard failed transaction state and begin a new OIDC exchange.", paths: ["apps/web/app/sign-in/page.tsx", "apps/web/app/api/auth/sign-in/route.ts"] },
      ],
      tests: ["apps/web/tests/identity-journeys.test.mjs"],
    },
    {
      id: "institution.first-activation",
      name: "Institution setup to tenant activation",
      actors: ["tenant owner", "institution administrator"],
      entry: "A tenant exists in provisioning state.",
      exit: "Activation readiness is recomputed transactionally and the tenant becomes active only when every blocking check passes.",
      browserRoute: "apps/web/app/admin/institution-setup",
      failureStates: ["missing institution", "missing campus", "missing academic period", "missing approved policy", "concurrent activation change"],
      steps: [
        { id: "institution-structure", action: "Create institution, campus and organisational structure within the active tenant.", paths: ["apps/web/app/admin/institution-setup", "apps/api/src/modules/institution-structure"] },
        { id: "academic-calendar", action: "Create and publish the academic period required by activation policy.", paths: ["apps/api/src/modules/institution-structure"] },
        { id: "policy-approval", action: "Approve the required institutional policies through versioned evidence.", paths: ["apps/api/src/modules/institution-structure"] },
        { id: "readiness-review", action: "Show every blocking and non-blocking activation check before the command is attempted.", paths: ["apps/web/src/features/institution-setup"] },
        { id: "activation-command", action: "Lock the tenant, recompute readiness and append activation audit and outbox evidence in one transaction.", paths: ["apps/api/src/modules/tenancy"] },
      ],
      tests: ["apps/api/tests/integration", "apps/web/tests"],
    },
    {
      id: "people.person-to-enrolment",
      name: "Person onboarding to active enrolment",
      actors: ["registrar", "institution administrator"],
      entry: "A learner identity must be onboarded individually or from a validated CSV file.",
      exit: "The canonical person, learner profile and effective enrolment are linked without duplicate identity evidence.",
      browserRoute: "apps/web/app/people",
      failureStates: ["duplicate identity", "invalid import row", "missing learner profile", "closed course run", "capacity unavailable", "concurrent person change"],
      steps: [
        { id: "person-create-or-import", action: "Create a person or stage a CSV dry run before committing canonical records.", paths: ["apps/web/src/features/people/people-workspace.tsx", "apps/api/src/modules/people"] },
        { id: "duplicate-review", action: "Resolve suspected duplicate records without destructive deletion.", paths: ["apps/web/app/people/duplicates", "apps/api/src/modules/people"] },
        { id: "learner-profile", action: "Create or activate the learner profile linked to the canonical person.", paths: ["apps/web/app/people/[personId]", "apps/api/src/modules/people"] },
        { id: "enrolment-create", action: "Validate institution, learner eligibility, course-run state and capacity before creating effective enrolment evidence.", paths: ["apps/api/src/modules/catalogue"] },
        { id: "learner-workspace", action: "Resolve the active enrolment into learner course and assignment views.", paths: ["apps/web/app/learning", "apps/api/src/modules/learner-course"] },
      ],
      tests: ["apps/api/tests", "apps/web/tests"],
    },
    {
      id: "curriculum.authoring-to-delivery",
      name: "Curriculum authoring to scheduled delivery",
      actors: ["curriculum manager", "independent approver", "course manager"],
      entry: "A programme or course definition is required.",
      exit: "An approved immutable blueprint version is referenced by a scheduled course run, offering and timetable.",
      browserRoute: "apps/web/app/courses",
      failureStates: ["validation error", "insufficient outcome coverage", "self-approval conflict", "stale version", "timetable conflict", "closed academic period"],
      steps: [
        { id: "draft-definition", action: "Create the programme or course blueprint draft and map learning outcomes and requisites.", paths: ["apps/api/src/modules/catalogue", "apps/web/app/courses"] },
        { id: "analysis-preview", action: "Run policy validation, outcome coverage and impact analysis before review submission.", paths: ["apps/api/src/modules/catalogue"] },
        { id: "independent-review", action: "Submit and independently approve one immutable curriculum version.", paths: ["apps/api/src/modules/catalogue"] },
        { id: "course-run-create", action: "Create a course run referencing the approved version and valid academic period.", paths: ["apps/api/src/modules/catalogue"] },
        { id: "offering-and-timetable", action: "Configure registration, capacity and conflict-checked timetable evidence.", paths: ["apps/api/src/modules/catalogue", "apps/web/app/courses"] },
      ],
      tests: ["apps/api/tests/integration", "apps/web/tests"],
    },
    {
      id: "learning.course-participation",
      name: "Course entry to completion progress",
      actors: ["learner", "instructor"],
      entry: "An active learner enrolment exists for an open or in-progress course run.",
      exit: "Learner activity and completion evidence are recorded and visible through the correct role-scoped workspace.",
      browserRoute: "apps/web/app/learning",
      failureStates: ["inactive enrolment", "unpublished content", "missing consent", "expired signed media URL", "progress write retry"],
      steps: [
        { id: "course-room-open", action: "Resolve learner ownership and open only published course content for the active run.", paths: ["apps/web/app/learning", "apps/api/src/modules/learner-course"] },
        { id: "governed-media", action: "Issue short-lived media delivery access only for ready and permitted assets.", paths: ["apps/api/src/platform/storage", "apps/worker/src/media-processor.ts"] },
        { id: "activity-progress", action: "Record idempotent activity and module progress for the current learner.", paths: ["apps/api/src/modules/learner-course"] },
        { id: "completion-view", action: "Show completion and released academic evidence without exposing another learner's records.", paths: ["apps/web/app/learning", "apps/web/app/gradebook"] },
      ],
      tests: ["apps/api/tests", "apps/web/tests/learning-delivery-integration.test.mjs"],
    },
    {
      id: "assessment.assignment-to-result",
      name: "Published assignment to released result",
      actors: ["instructor", "learner", "assessor", "moderator"],
      entry: "A governed assignment is drafted for a course run.",
      exit: "The learner can view a released result backed by immutable submission and marking evidence.",
      browserRoute: "apps/web/app/assessments",
      failureStates: ["assignment unpublished", "attempt limit reached", "group membership missing", "upload incomplete", "malware scan incomplete", "marker conflict", "moderation incomplete", "stale mark version"],
      steps: [
        { id: "assignment-publish", action: "Publish the assignment with due date, attempt policy, file rules, rubric and accommodations.", paths: ["apps/api/src/modules/academic-evidence", "apps/web/app/assessments"] },
        { id: "submission-start", action: "Start an individual or group attempt through owned active enrolment evidence.", paths: ["apps/api/src/modules/academic-evidence/application/learner-submission.service.ts"] },
        { id: "resumable-upload", action: "Register files, advance monotonic offsets and require complete clean scans before finalisation.", paths: ["apps/api/src/modules/academic-evidence", "scripts/qa/upload-reconnect.mjs"] },
        { id: "submission-receipt", action: "Finalise an immutable content snapshot with receipt number and SHA-256 checksum.", paths: ["apps/api/src/modules/academic-evidence/application/learner-submission.service.ts"] },
        { id: "mark-and-moderate", action: "Allocate markers, record rubric evidence and complete required moderation.", paths: ["apps/api/src/modules/academic-evidence"] },
        { id: "result-release", action: "Release the versioned mark and publish the learner grade result only after all gates pass.", paths: ["apps/api/src/modules/academic-evidence", "apps/web/app/gradebook"] },
      ],
      tests: ["apps/api/tests/runtime/assignment-session.test.mjs", "apps/api/tests/integration", "apps/web/tests"],
    },
    {
      id: "credentials.eligibility-to-verification",
      name: "Certificate eligibility to public verification",
      actors: ["registrar", "independent approver", "learner", "public verifier"],
      entry: "An approved certificate template and award rule exist.",
      exit: "An issued or revoked credential can be verified publicly without exposing private learner records.",
      browserRoute: "apps/web/app/evidence",
      failureStates: ["template unapproved", "learner ineligible", "duplicate issuance", "verification code unknown", "revocation reason missing"],
      steps: [
        { id: "template-governance", action: "Create, submit and independently approve a versioned certificate template.", paths: ["apps/api/src/modules/academic-evidence"] },
        { id: "award-evaluation", action: "Evaluate one learner against the active award rule and retain eligibility evidence.", paths: ["apps/api/src/modules/academic-evidence"] },
        { id: "certificate-issue", action: "Issue an immutable credential payload with a public verification code.", paths: ["apps/api/src/modules/academic-evidence", "apps/web/app/evidence"] },
        { id: "public-verification", action: "Return the minimum public verification result without requiring a tenant session.", paths: ["apps/web/app/verify", "apps/api/src/modules/academic-evidence"] },
        { id: "certificate-revoke", action: "Revoke with MFA, reason and durable public status evidence.", paths: ["apps/api/src/modules/academic-evidence"] },
      ],
      tests: ["apps/api/tests/integration", "apps/web/tests/evidence-room.test.mjs"],
    },
    {
      id: "exports.request-to-expiry",
      name: "Governed export request to verified expiry",
      actors: ["registrar", "institution administrator", "worker"],
      entry: "An authorised user requests a permitted institutional dataset or document.",
      exit: "The checksum-verified object is downloaded before automatic expiry, while export evidence remains auditable.",
      browserRoute: "apps/web/app/evidence/exports",
      failureStates: ["invalid filter", "worker lease loss", "render failure", "object-store failure", "checksum mismatch", "expired object", "unauthorised download"],
      steps: [
        { id: "export-request", action: "Validate format, dataset and filters before queueing the export job.", paths: ["apps/web/app/evidence/exports", "apps/api/src/modules/academic-evidence"] },
        { id: "export-claim", action: "Claim the job with a bounded worker lease and attempt number.", paths: ["apps/worker/src/export-processor.ts", "apps/api/database/migrations/0052_governed_document_exports.sql"] },
        { id: "export-render", action: "Build a tenant-scoped payload and deterministically render PDF, CSV or JSON bytes.", paths: ["apps/worker/src/export-document.ts"] },
        { id: "export-persist", action: "Persist bytes, checksum and object key before the worker-owned ready transition.", paths: ["apps/worker/src/export-processor.ts"] },
        { id: "export-download", action: "Retrieve through the authenticated BFF and verify SHA-256 before returning bytes.", paths: ["apps/web/app/api/academic-exports/[exportId]/download/route.ts", "apps/api/src/modules/academic-evidence/application/academic-export.service.ts"] },
        { id: "export-expiry", action: "Expire the object through the reconciled platform schedule while retaining metadata evidence.", paths: ["apps/worker/src/export-processor.ts", "apps/api/database/migrations/0053_platform_schedule_reconciliation.sql"] },
      ],
      tests: ["apps/worker/tests/export-document.test.ts", "apps/worker/tests/export-processor.test.ts", "apps/api/tests/integration/governed-document-export.test.mjs", "apps/web/tests/governed-exports.test.mjs"],
    },
    {
      id: "communications.intent-to-feedback",
      name: "Communication intent to provider feedback",
      actors: ["institution administrator", "worker", "communication provider"],
      entry: "An approved template and sender configuration exist for a permitted recipient set.",
      exit: "Every recipient delivery has a terminal provider status or bounded retry evidence.",
      browserRoute: "apps/web/app/communicate",
      failureStates: ["template unapproved", "required preference conflict", "provider timeout", "permanent rejection", "invalid webhook signature", "duplicate feedback"],
      steps: [
        { id: "intent-create", action: "Create an idempotent notification intent from an approved template and bounded recipient definition.", paths: ["apps/api/src/platform/communications", "apps/web/app/communicate"] },
        { id: "recipient-routing", action: "Resolve policy, channel preferences and sender configuration for each recipient.", paths: ["apps/worker/src/notification-router.ts"] },
        { id: "delivery-attempt", action: "Deliver through the selected provider with bounded retry and terminal failure handling.", paths: ["apps/worker/src/notification-dispatcher.ts"] },
        { id: "provider-feedback", action: "Verify provider webhook signatures and apply idempotent delivery feedback.", paths: ["apps/api/src/platform/communications"] },
      ],
      tests: ["apps/worker/tests", "apps/api/tests", "apps/web/tests"],
    },
    {
      id: "storage.upload-to-retention",
      name: "Governed upload to retention or approved deletion",
      actors: ["authorised user", "worker", "institution administrator"],
      entry: "A user requests an upload in a permitted namespace with available quota.",
      exit: "The ready asset is retained under policy or deleted only after an approved evidence-bearing request.",
      browserRoute: "apps/web/app/admin/storage",
      failureStates: ["quota exceeded", "offset mismatch", "checksum mismatch", "malware detected", "processing failure", "retention hold", "deletion approval denied"],
      steps: [
        { id: "upload-create", action: "Validate namespace, quota, media policy and expected object metadata.", paths: ["apps/api/src/platform/storage"] },
        { id: "resumable-transfer", action: "Advance upload offsets monotonically and verify final checksum.", paths: ["apps/api/src/platform/storage", "scripts/qa/upload-reconnect.mjs"] },
        { id: "scan-and-process", action: "Require malware disposition and create governed media renditions, captions and transcripts.", paths: ["apps/worker/src/media-processor.ts"] },
        { id: "signed-delivery", action: "Issue short-lived delivery access only after tenant and permission checks.", paths: ["apps/api/src/platform/storage"] },
        { id: "retention-or-deletion", action: "Apply retention policy or execute an individually approved deletion request with evidence.", paths: ["apps/api/src/platform/storage", "apps/web/app/admin/storage"] },
      ],
      tests: ["apps/api/tests", "apps/worker/tests", "scripts/qa/upload-reconnect.mjs"],
    },
    {
      id: "control.support-elevation",
      name: "Customer-approved support elevation",
      actors: ["tenant owner", "platform operator", "worker"],
      entry: "A support case requires temporary privileged assistance.",
      exit: "The bounded support session expires or is forcibly terminated and all privileged actions remain attributable.",
      browserRoute: "apps/control-plane/app/support",
      failureStates: ["customer approval missing", "MFA missing", "scope mismatch", "session expired", "forced termination", "tenant suspended"],
      steps: [
        { id: "support-request", action: "Create the support request with tenant, purpose, scope and expiry.", paths: ["apps/control-plane/src"] },
        { id: "customer-approval", action: "Require explicit tenant approval before elevation can become active.", paths: ["apps/api/src/modules/tenant-entitlements", "apps/control-plane/src"] },
        { id: "operator-assurance", action: "Require the separate operator OIDC client, platform role and MFA assurance.", paths: ["apps/control-plane/app/sign-in", "apps/control-plane/src/server"] },
        { id: "bounded-operation", action: "Record every operation against the support session and approved purpose.", paths: ["apps/control-plane/src"] },
        { id: "session-expiry", action: "Expire or terminate the session through the platform schedule and invalidate further access.", paths: ["apps/worker/src/scheduler.ts", "apps/api/database/migrations/0053_platform_schedule_reconciliation.sql"] },
      ],
      tests: ["apps/control-plane/tests", "apps/worker/tests"],
    },
  ],
};
