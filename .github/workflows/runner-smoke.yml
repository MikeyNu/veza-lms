name: runner-smoke

on:
  pull_request:

permissions: {}

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: node --version
      - run: echo "GitHub runner is operational"
